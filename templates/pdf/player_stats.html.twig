<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Player Stats - {{ player.nickname }}</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        .header { text-align: center; border-bottom: 2px solid #333; padding-bottom: 20px; margin-bottom: 30px; }
        .player-info { display: flex; align-items: center; margin-bottom: 30px; }
        .avatar { width: 80px; height: 80px; border-radius: 50%; background: #6366f1; color: white; display: flex; align-items: center; justify-content: center; font-size: 32px; margin-right: 20px; }
        .stats-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 15px; margin-bottom: 30px; }
        .stat-card { border: 1px solid #ddd; padding: 15px; text-align: center; border-radius: 8px; }
        .stat-value { font-size: 24px; font-weight: bold; color: #6366f1; }
        .stat-label { color: #666; margin-top: 5px; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { border: 1px solid #ddd; padding: 10px; text-align: left; }
        th { background: #6366f1; color: white; }
        .footer { margin-top: 40px; text-align: center; color: #666; font-size: 12px; }
    </style>
</head>
<body>
    <div class="header">
        <h1>NEXUSPLAY - Player Statistics Report</h1>
        <p>Generated on {{ generatedAt|date('F d, Y H:i') }}</p>
    </div>

    <div class="player-info">
        <div class="avatar">{{ player.nickname|first|upper }}</div>
        <div>
            <h2>{{ player.nickname }}</h2>
            <p>{{ player.game.name }} • {{ player.role|default('No Role') }}{% if player.team %} • {{ player.team.name }}{% endif %}</p>
            <p>Rank: {{ player.getRank() }} • Score: {{ player.score }} ELO</p>
        </div>
    </div>

    <h3>Performance Statistics</h3>
    <div class="stats-grid">
        <div class="stat-card">
            <div class="stat-value">{{ statistics ? statistics.matchesPlayed : 0 }}</div>
            <div class="stat-label">Matches Played</div>
        </div>
        <div class="stat-card">
            <div class="stat-value">{{ statistics ? statistics.wins : 0 }}</div>
            <div class="stat-label">Wins</div>
        </div>
        <div class="stat-card">
            <div class="stat-value">{{ statistics ? statistics.winRate : 0 }}%</div>
            <div class="stat-label">Win Rate</div>
        </div>
        <div class="stat-card">
            <div class="stat-value">{{ statistics ? statistics.kills : 0 }}</div>
            <div class="stat-label">Kills</div>
        </div>
        <div class="stat-card">
            <div class="stat-value">{{ statistics ? statistics.deaths : 0 }}</div>
            <div class="stat-label">Deaths</div>
        </div>
        <div class="stat-card">
            <div class="stat-value">
                {% if statistics and statistics.deaths > 0 %}
                    {{ ((statistics.kills + statistics.assists) / statistics.deaths)|number_format(2) }}
                {% else %}
                    {{ statistics ? (statistics.kills + statistics.assists) : 0 }}
                {% endif %}
            </div>
            <div class="stat-label">KDA</div>
        </div>
    </div>

    {% if recentMatches|length > 0 %}
    <h3>Recent Matches</h3>
    <table>
        <thead>
            <tr>
                <th>Date</th>
                <th>Map</th>
                <th>Kills</th>
                <th>Deaths</th>
                <th>Assists</th>
                <th>KDA</th>
                <th>Result</th>
            </tr>
        </thead>
        <tbody>
            {% for matchPlayer in recentMatches %}
            {% set match = matchPlayer.getGameMatch() %}
            <tr>
                <td>{{ match.matchDate ? match.matchDate|date('M d, Y') : 'N/A' }}</td>
                <td>{{ match.map|default('Unknown') }}</td>
                <td>{{ matchPlayer.kills }}</td>
                <td>{{ matchPlayer.deaths }}</td>
                <td>{{ matchPlayer.assists }}</td>
                <td>
                    {% if matchPlayer.deaths > 0 %}
                        {{ ((matchPlayer.kills + matchPlayer.assists) / matchPlayer.deaths)|number_format(2) }}
                    {% else %}
                        {{ matchPlayer.kills + matchPlayer.assists }}
                    {% endif %}
                </td>
                <td>{{ matchPlayer.isWinner ? 'Win' : 'Loss' }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    {% endif %}

    <div class="footer">
        <p>NEXUSPLAY - Esports Management Platform</p>
        <p>This report was automatically generated on {{ generatedAt|date('F d, Y') }}</p>
    </div>
</body>
</html>

